# 검증 안내

## 자동 검사

의존성이 설치된 프로젝트 루트에서 순서대로 실행한다.

```bash
npm test
npm run typecheck
npm run build
```

`npm test`는 Node.js 기본 테스트 실행기로 다음 순수 로직을 검증한다.

| 파일 | 범위 |
|---|---|
| `tests/mobile-calendar.test.cjs` | 동시간·부분 겹침 배치, 삭제 예약 제외, 운영 시간 경계, 원본 불변성, 30분 선택 |
| `tests/mobile-duration.test.cjs` | 2·3시간 및 사용자 지정 길이, 다른 방 예약, 인접 예약, 운영 시간, 원본 보존 |
| `tests/validation.test.cjs` | 등록된 회의실, 미등록·상속 속성 이름 거부, 30분 단위와 시간 범위 |

이 검사는 브라우저 조작이나 실제 HTTP 저장을 실행하지 않는다. 별도 lint 명령은 등록되어 있지 않다. 새 의존성 없이 타입 검사·빌드와 아래 QA를 함께 수행한다.

## 로컬 QA 서버

빌드를 완료한 뒤 다음 명령을 같은 터미널에서 실행한다. 매번 새 임시 디렉터리를 만들고 Redis 변수 네 개를 빈 값으로 지정하여 로컬 파일 저장소로 분리한다. `.env.local`에 운영 연결이 있어도 이 프로세스는 지정한 빈 환경변수를 사용한다.

```bash
MEETING_QA_DIR="$(mktemp -d)"
DATA_DIR="$MEETING_QA_DIR" \
UPSTASH_REDIS_REST_URL= UPSTASH_REDIS_REST_TOKEN= \
KV_REST_API_URL= KV_REST_API_TOKEN= \
npm run start -- --hostname 127.0.0.1 --port 3134
```

브라우저에서 `http://127.0.0.1:3134`를 연다. 합성 예약만 생성하고 이름·메모에 실제 사람이나 운영 정보를 넣지 않는다. 서버를 종료해도 임시 디렉터리의 파일은 남는다. 운영 데이터 파일이나 Redis 설정을 덮어써서 테스트하지 않는다.

## 화면 QA

| 화면 | 확인할 동작 |
|---|---|
| 데스크톱 | 64px 상단 한 줄의 로고·주 이동·월 제목·예약 내역, 256px 사이드바, 예약 만들기와 미니 달력의 같은 너비, 인원수·이름 입력칸 생략, 미니 달력의 선택 주, 긴 예약자 목록의 내부 스크롤 없음 |
| 주간 시간표 | 48px 헤더에서 요일·날짜 한 줄 표시, 시간당 64px 간격, 빈 시간 클릭·드래그와 현재 시간선의 시간 일치, 30분·1시간 예약의 방·시간 표시, 두 방의 완전·부분 겹침, 이전·다음 주와 오늘 이동 |
| 모바일 | 3일/1일 전환, 전체/방별 필터, 날짜 직접 이동, 날짜별 하루 예약 건수, 세로 스크롤 중 오작동 없음 |
| 모바일 예약 | 2시간·3시간·2시간 30분 선택, 다음 예약을 넘는 길이 비활성화, 22:00 종료 |
| 상세·입력 | 생성·수정·삭제, 저장 뒤 화면 갱신, 오류 표시, 긴 제목·메모, 예약자 이름 재입력 없이 다음 예약 |
| 키보드 | 대화상자의 최초 초점, Tab·Shift+Tab 이동, Escape 닫기와 초점 복귀 |
| 내역 | 전체·예정·지난 예약·삭제 필터, 검색, 날짜별 묶음, 삭제 기록 유지 |

데스크톱은 960px·1280px·1440px 폭에서 상단 줄바꿈·가로 넘침과 좁은 열의 예약 표시를 확인한다. 모바일은 360px·390px·430px 폭에서 확인한다. 브라우저의 화면 크기 에뮬레이션만 통과한 경우 실제 iOS·Android 검증과 구분해서 기록한다.

## HTTP QA와 데이터 보존

격리된 서버에 합성 예약을 만들고 다음 응답과 후속 조회를 확인한다.

1. 정상 예약 생성은 201, 같은 방의 겹치는 예약은 409, 다른 방의 동시간 예약은 201이다.
2. 등록되지 않은 회의실과 운영 시간 밖·30분 단위 밖 입력은 400이다.
3. 수정 결과를 다시 조회하고 삭제 후 `deletedAt`과 원래 예약 정보가 남는지 확인한다.
4. 지난 예약과 삭제 이력이 변경 전후 유지되고, 삭제된 예약의 시간을 다시 예약할 수 있는지 확인한다.

로컬 파일 백엔드 QA는 운영 Redis 연결이나 여러 서버 인스턴스의 동시 쓰기를 검증하지 않는다. 현재 쓰기 큐는 프로세스 내부만 직렬화한다. 계정 없는 이용 정책도 그대로이며, 이름 기억 기능은 접근 권한 검사가 아니다.

배포 검증에서는 기존 연결 설정 유지와 화면·내역 조회를 확인한다. 운영 예약을 생성·수정·삭제하는 시험을 로컬 QA와 혼용하지 않는다. 검사 결과에는 실행한 명령, 성공·실패, 사용한 환경, 실제 기기 검증 여부를 기록한다.
